import 'package:flutter/material.dart';

/// Destiny brand palette — keep in sync with [AppTheme].
class AppColors {
  static const Color primary = Color(0xFF0D47A1); // Deep blue from logo
  static const Color accent = Color(0xFFD32F2F); // Brand red (emphasis / CTA)
  static const Color background = Color(0xFFF7F8FA); // Very light neutral
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceAlt = Color(0xFFEEF3F8); // Cool blue-grey band
  static const Color navy = Color(0xFF0A2540); // Dark editorial section
  static const Color textPrimary = Color(0xFF1A202C);
  static const Color textSecondary = Color(0xFF5B6B7C);
  static const Color border = Color(0xFFE2E8F0);
  static const Color imagePlaceholder = Color(0xFFEDF2F7);
}
