# Destiny customer commerce Edge Function (M3B.5).
# verify_jwt = true: gateway rejects non-Supabase JWTs.
# Function still calls auth.getUser(token) and derives user_id server-side.

[functions.customer-api]
verify_jwt = true
