# Destiny staff commerce Edge Function (M3B.5).
# verify_jwt = true: callers must present a Supabase Auth access token.
# Staff role still resolved only via staff_users.user_id after getUser.

[functions.staff-commerce-api]
verify_jwt = true
